# Политика безопасности

## Применимые версии

| Версия | Поддерживается |
|--------|---------------|
| 1.1.x  | ✅ Да          |
| 1.0.x  | ❌ Нет         |

## Известные меры безопасности

### Аутентификация
- Панель `/admin` и все `/api/*` маршруты защищены HTTP Basic Auth
- Пароль задаётся в `ADMIN_PASSWORD` через `.env`
- **Обязательно использовать HTTPS** — Basic Auth не шифрует credentials

### Rate Limiting
- Эндпоинт `/scan/:id` — 10 запросов в минуту с одного IP
- Превышение: HTTP 429

### Токены и секреты
- Все токены хранятся в `.env` (в `.gitignore`)
- `config.js` не содержит секретов в коде
- Аудит-лог не записывает значения токенов

### Персональные данные
- Данные несовершеннолетних: требуется согласие родителей (`consentDate`)
- GDPR-удаление: полное удаление без возможности восстановления
- Срок хранения: автоочистка по `DATA_RETENTION_YEARS`
- Передача данных: только Green API и Telegram (уведомления)

### QR-коды
- ID учеников — UUID v4 (сложно угадать)
- Защита от двойного скана: 10 минут блокировки
- Страница скана: `noindex, nofollow`

## Сообщить об уязвимости

Если вы обнаружили уязвимость:
1. **Не создавайте публичный Issue**
2. Опишите уязвимость владельцу системы напрямую
3. Укажите: описание, шаги воспроизведения, возможные последствия
4. Ожидайте ответа в течение 72 часов

## Рекомендуемые дополнительные меры

- [ ] Замена Basic Auth на сессионную аутентификацию
- [ ] CSRF-защита для POST/PUT/DELETE запросов
- [ ] Шифрование `db.sqlite` на уровне файловой системы
- [ ] Настройка fail2ban для защиты от перебора паролей
- [ ] Регулярные бэкапы `data/db.sqlite`
